[package]
name = "mtprsto"
version = "0.1.0"
edition = "2024"

[dependencies]
aes = "0.9"
base64 = "0.23.1"
cipher = "0.5"
flate2 = "1"
futures-util = { version = "0.3", optional = true }
hmac = "0.13"
md-5 = "0.11"
mime_guess = "2"
num-bigint = { version = "0.5", features = ["rand_0_10"] }
num-traits = "0.2"
pbkdf2 = "0.13"
rand = "0.10"
rustls-native-certs = { version = "0.8", optional = true }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
sha1 = "0.11"
sha2 = "0.11"
tokio = { version = "1", features = ["full"] }
tokio-rustls = { version = "0.26", optional = true }
tracing = { version = "0.1", features = ["log"] }
tracing-subscriber = "0.3"

[dependencies.tokio-tungstenite]
version = "0.30"
features = ["rustls-tls-native-roots"]
optional = true

[features]
default = []
# Compiles all tracing macros to no-ops (STATIC_MAX_LEVEL = OFF) for
# latency-sensitive deployments that don't want observability overhead.
tracing-off = ["tracing/max_level_off"]
ws = [
  "dep:futures-util",
  "dep:rustls-native-certs",
  "dep:tokio-rustls",
  "dep:tokio-tungstenite",
]
