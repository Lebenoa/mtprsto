# docs-layer branch

Tracks the OFFICIALLY PUBLISHED layer (223 — the number
core.telegram.org/schema documents; scraped into tools/schema.meta.json
at fetch time).

- `API_LAYER = 223` (src/api.rs)
- `tools/gentl.py` CTOR_ALIASES maps every constructor re-issued between
  223 and 229 (generated by tools/gen_docs_aliases.py from the official
  tl.json + the 229 schema), so the generated parsers accept both ids.
- PENDING: a few curated parsers still read 225/229 FIELD SHAPES
  (dialog unread_poll_votes_count, 229 keyboardInlineButton structure).
  Wire-verify at layer 223 and reconcile before production use.
